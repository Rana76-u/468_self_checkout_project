# Self-Checkout System — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Note: This project is designed to run on Google Colab with GPU acceleration.
# For local installation, ensure CUDA-compatible drivers are installed for
# real-time inference performance with YOLOv10x.

# ── Core Detection & Tracking ─────────────────────────────────────────────────
ultralytics>=8.4.0          # YOLOv10x object detection (includes torch dependency)
deep-sort-realtime>=1.3.2   # DeepSORT multi-object tracking with persistent IDs

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python>=4.10.0       # Frame capture, video I/O, bounding box rendering

# ── Image Processing ──────────────────────────────────────────────────────────
Pillow>=10.0.0              # RGBA receipt overlay compositing on video frames

# ── Numerical Computing ───────────────────────────────────────────────────────
numpy>=1.26.0               # Array operations for frame and detection data

# ── Data Analysis & Visualization ─────────────────────────────────────────────
pandas>=2.1.0               # Per-frame detection logs and post-run DataFrames
matplotlib>=3.8.0           # Detection charts, confidence histograms, stability plots
seaborn>=0.13.0             # Styled distribution plots for confidence analysis
